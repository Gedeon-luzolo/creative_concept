import Loading from "@/src/components/Loading";

/**
 * Composant de chargement global pour Next.js
 * Utilisé automatiquement lors de la navigation entre les pages
 */
export default function LoadingPage() {
  return <Loading />;
}
